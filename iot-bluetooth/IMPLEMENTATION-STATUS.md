# 功能实现状态

本文按需求文档和当前代码核对，不把“代码存在”当成“实车已经验证”。当前 App 基线为 Build 17，线上服务在本轮累计部署前仍为 `iot-remote` revision `05043e0`。

## 已实现

| 功能 | 当前状态 |
| --- | --- |
| BLE 扫描、认证、状态读取、开锁、关锁 | 已实现，主开关锁已实车验证 |
| BLE 设置、服务器、APN、RFID、外部锁、旧数据、日志、OTA | 已有界面和协议实现，部分能力尚未逐项实车验证 |
| 云端配对与认证 | 已实现一次性配对码、Keychain、Secure Enclave 签名、随机数防重放 |
| 远程声音找车 | 已实现并实车验证，包含 10 秒冷却 |
| 远程开锁 | 已实现并实车验证，包含长按、Face ID、R0、L0、幂等和超时保护 |
| 远程关锁 | Build 7 已实现，包含长按、Face ID、签名停稳确认、R0、L1、幂等和物理结果提示，等待新版 App 实车验收 |
| S6 设备信息 | 已保存电量、回包时间和全部原始字段，只有电量完成协议确认 |
| D0 单次定位 | 用户确认指令有效；Build 9 服务端已部署度分转 WGS84、有效与无效点保存及查询 API，MapKit App 与真实坐标待验收 |
| App 信息架构 | Build 8 已改为车辆、地图、记录、更多四页，首页支持远程与 BLE 显式通道选择 |
| 指令记录 | 已在“记录”页展示远程指令、时间和终态 |
| 双找车入口 | Build 9 首页并排提供声音找车与定位找车，定位完成后刷新服务端位置 |
| 锁状态统一 | Build 10 实现 BLE 近场优先、来源标识、冲突提示和签名同步，服务端已部署；Build 12 已安装并启动，页面与 BLE 行为待验收 |
| 在线状态与部署读回 | 已部署 420 秒通信静默、720 秒离线、控制禁用和 `/healthz` revision 读回 |
| 定位质量过滤 | 已实现卫星、HDOP、设备时间、乱序和异常速度过滤，被拒绝点保留但不进入地图，待真实 D0 样本校准阈值 |
| D1 定位策略 | Build 15 与 revision `043004d` 已部署，支持开锁 60 秒、物理关锁确认后 3600 秒、告警 300 秒、新会话 H0 协调、期望值与确认值分离及超时不重发。完成度审计已修复逻辑 L1 或 H0 关锁在现场确认前提前降频的问题；设备 D1 回包待验收 |
| 布防与异常移动告警 | Build 15 与 revision `043004d` 已部署并安装，支持物理关锁确认后的 5 分钟等待、手动布防、W0 等待期抑制、活动告警合并、前台声音触感卡片、解除告警，以及 D1 300 秒后串行 D0；真实 W0 仍待验收 |
| 骑行、轨迹与保留 | Build 15 与 revision `043004d` 已部署并安装，支持开锁开始、关锁结束、重启与 H0 恢复、可靠点计数和估算距离、10 分钟缺口断线、历史地图、7 天或 30 天设置及每日 03:30 整段清理。完成度审计已确保活动告警及其完整事件链不被 30 天清理误删；真实 60 秒轨迹仍待验收 |
| IoT 离线移动推断 | Build 15 与 revision `043004d` 已部署并安装，支持断网前可信停车基线、新会话 H0 关锁门禁、两次串行 D0、重复位置拒绝、超时不重试和推断告警；完成度审计已补充检查期间 H0 开锁立即终止和数据库评估时关锁双重门禁。默认采用 200 米位移与 75 米样本一致性阈值，待户外样本校准 |
| BLE 操作事件补报 | Build 15 与 revision `043004d` 已部署并安装：成功回包与锁状态回读一致后本机持久化，签名上传、相同 UUID 幂等、2xx 后删除、24 小时外只审计、新状态优先、IoT 离线无下行、在线只协调 D1；真实 BLE 在线上传、断网重启和联网补报待人工验收 |
| 指令中心、审计、设备会话与设置 | Build 16 与 revision `7521515` 已部署并安装：服务端静态审核目录与 App 内置 BLE 目录合并展示，未验证、内部、危险和不适用能力不生成执行按钮；设备会话保存随机盐地址摘要、收发与解析计数、Q0/H0 时间和结束原因。完成度审计已修复自然断线闭合，补齐位置历史、单次轨迹、审计和会话的敏感读取审计，并把 15 秒前台轮询收窄到车辆、能力、命令和告警，避免重复敏感读取。Build 17 补齐实际 BLE 回包的最近结果与安全摘要，不展示旧骑行正文、日志正文、配置或认证材料；无签名与签名编译、真机安装、Build 17 读回和启动均已通过。设置页只允许修改轨迹保留期，其余服务阈值只读。累计服务版本尚待本轮部署，页面内容仍待人工验收。 |
| 每日加密恢复副本 | revision `05043e0` 已部署：03:30 清理后于 03:45 生成 SQLite 在线快照，只保留原子替换的最新 CMS AES-256 密文；服务器只保存公钥证书，私钥只在管理员 Mac 登录钥匙串。定时器已启用并保持活动，首次任务、审计和权限检查通过；真实服务器密文已在 Mac 完成钥匙串解密、18 张表与 SQLite 完整性恢复演练，临时明文已清理。 |

## 尚未实现

1. 可靠的自动静止判断和机械锁电子确认。当前远程关锁依赖用户确认停稳，逻辑关锁后仍需现场核对并单独确认，才启动布防等待。
2. 后台即时通知。免费 Personal Team 阶段只实现 App 前台轮询提醒和重新打开后的活动告警读取。

## 建议顺序

第一、二、三优先级的当前代码已通过 77 项远程服务测试，Build 17 已完成无签名与签名编译、真机安装、启动和版本读回。完成度审计新增的累计服务修复尚待部署到线上并执行当前密文恢复演练，在完成远端测试、公网 revision 和独立恢复读回前不把本轮标为完成。之后仍需用户可操作时验收指令中心页面、BLE 事件真机队列以及真实 D0 地图、60 秒轨迹、D1 回包、W0 云端告警和离线移动阈值。
