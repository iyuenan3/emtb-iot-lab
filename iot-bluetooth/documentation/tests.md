# 测试策略

## Build 33 当前结果

| 范围 | 结果 |
| --- | --- |
| 官方认证向量与 CRC8 | 通过 |
| 帧编码与解码往返 | 通过 |
| 开锁用户 ID、时间戳大端载荷 | 通过 |
| 关锁与必要回执载荷 | 通过 |
| 认证、初始化、控制两种回调先后顺序 | 通过 |
| 600 毫秒、1 秒、800 毫秒三个等待窗口 | 通过 |
| 不完整控制响应拒绝 | 通过 |
| 旧源码删除与 Xcode Target 白名单 | 通过 |
| 用户可见车辆动作只有开锁和关锁 | 通过 |
| App 与 MacBook 工具共用状态机 | 通过 |
| MacBook 工具独立编译 | 通过 |
| iPhoneOS 无签名完整编译 | `BUILD SUCCEEDED` |
| 时序变异测试 | 将控制前等待改为 0 后，测试按预期失败 |
| Apple Development 签名 | 严格签名校验通过，arm64，描述文件包含目标 iPhone |
| iPhone 安装与读回 | 已安装，设备端显示 Bundle Version 33 |
| iPhone 单次开锁 | 通过，仪表稳定常亮、晃动无蜂鸣、蓝牙自动断开 |
| iPhone 单次关锁 | 通过，仪表熄灭、晃动有蜂鸣、蓝牙自动断开 |

运行自动测试：

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v
```

## 真机验收结论

1. 从正确关锁基线长按一次开锁，仪表稳定常亮、晃动无蜂鸣、蓝牙自动断开。
2. 随后长按一次关锁，仪表熄灭、晃动恢复蜂鸣、蓝牙自动断开。
3. Build 33 的当前蓝牙开关锁范围验收完成。

自动测试、编译、安装和协议回包都不能证明车辆物理动作成功。
