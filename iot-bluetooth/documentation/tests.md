# 测试策略

## 已有自动化

| 范围 | 命令 | 当前覆盖 |
| --- | --- | --- |
| 远程服务 | `cd ../iot-remote && PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` | 77 项，覆盖签名、BLE 状态同步、BLE 事件幂等、时效、冲突与离线下行抑制、数据库、D0 转换与去重、定位质量、物理关锁后的 D1 门禁、新会话协调、布防告警、骑行生命周期、历史与保留清理、活动告警事件保留、精确能力目录、统一审计、敏感明细读取、设备会话替换与计数、自然断线闭合、隐私摘要、命令状态机、通信静默、部署版本读回、加密恢复副本，以及真实活动 TCP 设备连接下的优雅停机 |
| iOS 源码守卫 | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` | 4 项，固定 Build 号、外部锁无发送入口、BLE 高风险原始写方法私有，以及页面只能调用设备所有者验证入口 |
| iOS 编译 | `xcodebuild -project IoTBluetooth.xcodeproj -scheme IoTBluetooth -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO build` | Swift 类型检查、资源和工程配置 |

iOS 工程目前没有 XCTest 或 XCUITest Target。自动构建不能证明 CoreBluetooth、Face ID、真机网络或车辆物理动作正确。

Build 17 的无签名与签名真机目标编译均已通过，已安装到唯一符合条件的开发真机，读回版本 1.0、Build 17 并成功启动。静态类型检查覆盖 BLE 最近结果字典、服务端与本地目录合并、指令中心环境注入和安全摘要构造；安装与启动仍不能证明页面渲染或真实 BLE 回包。

2026 年 8 月 22 日，Build 11 完成签名构建、真机安装和系统启动。远端 revision `3d295e4` 在 Python 3.12 通过 38 项测试，并在真实活动 IoT 长连接下完成一次 `Result=success` 停止。以上证据不替代 App 页面、BLE 动作或 D1 回包的人工验收。

同日，Build 12 完成签名构建、真机安装和系统启动。远端 revision `dae2fc8` 在 Python 3.12 通过 49 项测试，SQLite 告警迁移和源码哈希一致，公网健康接口读回相同 revision，未认证 API 为 401，站点首页为 200。设备在新版本上自动重连，活动长连接下停止仍为 `Result=success`。真实 W0、D1 与告警页面继续待人工验收。

同日，Build 13 完成签名构建并成功安装，自动启动因设备锁屏被系统拒绝。远端 revision `5002f2f` 在 Python 3.12 通过 54 项测试，SQLite 骑行、设置和清理迁移及四个核心源码哈希一致，公网健康接口读回相同 revision，未认证 API 为 401，站点首页为 200。设备完成两次自动重连，活动长连接下停止仍为 `Result=success`，回滚源码、unit 和 SQLite 备份齐全。真实 60 秒轨迹、历史页面与保留设置交互继续待人工验收。

Build 14 完成无签名与签名真机目标编译、真机安装和系统启动。revision `5d0ef70` 在本机与远端 Python 3.12 均通过 60 项测试；SQLite 离线基线与告警比较迁移、两个 unit 阈值、四个核心源码哈希、权限和回滚材料全部核对通过。设备完成两次自动重连，活动长连接停止为 `Result=success`；公网读回相同 revision，未认证 API 为 401，站点首页为 200。协议模拟覆盖两次可信定位创建推断告警、重复或不一致位置拒绝、D0 超时同一会话不重试，以及已有 D1 在途时串行等待。以上证据不代表默认 200 米与 75 米阈值已经完成户外校准。

Build 15 完成无签名与签名真机目标编译，安装到唯一已配对且启用开发者模式的 iPhone，从真机读回 Build 15 并成功启动。revision `043004d43e96a526e24bdd7fe662537b1320a71c` 在本机、远端发布目录和线上目录均通过 65 项测试。部署完成优雅停机 `Result=success`、`ble_events` 十个字段迁移、四个核心源码哈希、权限、两个离线阈值和回滚材料核对。公网读回相同 revision，未认证 API 为 401，首页为 200，设备已自动恢复在线。新增测试覆盖 BLE 事件全局去重、24 小时外只审计、旧状态不覆盖新状态、失败或回读不一致不生效、幂等键不一致拒绝、IoT 离线无下行和在线只创建 `tracking.set_policy`。真实 BLE 队列持久化、断网重启和联网补报仍待人工验收。

Build 16 在本机、远端发布目录和线上目录通过 69 项测试，并完成无签名与签名真机目标编译、真机安装、启动和 Build 16 读回。revision `7521515a144e80fa0395f93833e6adff44f1e833` 部署时核对了优雅停机 `Result=success`、`device_sessions` 与 `audit_logs` 迁移、四个核心源码哈希、700/600/600 权限、200/75 阈值、备份和不可变发布目录。公网读回相同 revision，未认证 API 为 401，首页为 200，设备自动恢复在线，线上数据库只有一条活动摘要会话且已有审计记录。新增测试覆盖至少 30 项服务端静态能力、危险维护禁用、最后结果安全摘要、配对、命令、设置与定位审计、认证材料和定位原始字段不进入审计、设备会话替换、收发与解析计数、Q0/H0 时间、服务重启关闭遗留会话，以及审计和会话 API 不返回原始地址。页面内容仍待人工验收。

每日加密恢复副本新增 3 项自动测试，覆盖 SQLite 在线快照、最新密文原子替换、密文往返、错误私钥拒绝且不产生恢复目标，以及审计仅包含密文时间、大小和摘要等安全元数据。revision `05043e01122062eba33a45a8b7b811eed04e1097` 已在远端发布目录和线上目录分别通过 72 项测试，五个源码哈希一致；主服务优雅停止为 `Result=success`，备份 timer 已启用并保持活动，首次 oneshot 为 `Result=success` 与退出码 0。恢复目录无明文，目录、密文、元数据、锁文件、公钥和两个 unit 权限已核对。真实服务器密文下载后由登录钥匙串解密，恢复工具验证了摘要、18 张表和 `PRAGMA integrity_check`，并清理全部临时明文。公网读回相同 revision 且设备在线，未认证 API 为 401，首页为 200。

严格完成度审计新增 3 项反例测试：对端自然 EOF 必须闭合设备会话并把在途命令标为 `connection_lost`；离线移动双定位期间 H0 变为开锁必须立即终止；数据库最终评估即使距离满足阈值，也禁止为开锁车辆创建推断告警。以上测试只证明代码门禁，不替代真实断网和户外阈值验收。

定位与保留审计进一步修改 6 项既有 D1 测试并新增 1 项清理反例。逻辑 L1 或 H0 关锁且安全状态仍为撤防时不得发送 3600 秒 D1；现场确认物理关锁进入等待期后才创建该策略命令，期望值与原设备确认值继续分离。超过 30 天的无关联告警事件会被删除，但活动告警自身的早期事件必须完整保留。

敏感读取审计新增 1 项测试并强化 2 项既有测试。服务端能力目录必须与 35 个审核 ID 精确一致；位置历史、单次轨迹、审计和设备会话读取必须留下动作记录，且令牌与定位原始字段不得进入审计。App 通用 iOS 编译同时证明 15 秒轮询只调用运行态刷新，不重复拉取敏感明细。

最终需求对照修正外部锁反例。服务端 `wheel_lock` 和 App `ble.81` 必须标记为“危险维护”和“因果未确认”，不能因为 L5 与 BLE 0x81 无回包就标记为“不适用”。维护页只保留禁用说明，不存在可触发 `operateExternalLock` 的按钮或确认弹窗；自动测试固定服务端标签、禁用原因和精确能力 ID，iOS 编译固定 App 数据结构和禁用页面。

Build 18 完成无签名与签名真机目标编译，安装到唯一已配对且启用开发者模式的 iPhone，读回版本 1.0、Build 18 并成功启动。设备选择、安装、查询和启动输出均被收敛为计数与结果，不在验收记录中保留设备标识。该证据证明禁用边界可编译并能被系统加载，不替代页面文字的人工检查。

Build 20 将 BLE 开关锁、车辆设置、RFID、电源、旧数据清理、服务器、APN、OTA、设备密钥和维护密钥写操作统一收口到 `deviceOwnerAuthentication`。原始写方法改为私有，页面只调用带 `WithOwnerAuthentication` 后缀的公开入口。认证开始后立即进入统一忙状态，结束前拒绝并发重复请求。4 项源码守卫与无签名通用 iOS 编译已通过，签名安装和身份验证弹窗的人机交互仍按不同证据层分别记录。

Build 20 同时完成签名真机目标编译，安装到唯一符合条件的开发 iPhone，读回版本 1.0、Build 20 并成功启动。该过程没有执行 BLE 写操作，也没有触发身份验证弹窗；安装和启动证明交付物可加载，弹窗文案、取消路径、验证通过后的单次发送仍需人工交互验收。

## 已完成实车验收

- BLE 开锁和关锁会联动仪表、动力与轮毂锁。
- 远程开锁链路已完成实车验收。
- 远程声音找车已成功发声。
- S6 能读取车端电量和原始字段。

## 待验收

- Build 9 MapKit 车辆标记、双找车按钮、首页远程关锁入口和户外可读性。
- Build 12 的 BLE 状态优先、冲突提示、服务器同步、断开回退、D1 状态显示、布防和告警页面。
- Build 13 的 60 秒轨迹、骑行历史地图、估算距离和保留设置交互。
- Build 14 的真实断网、重连双定位、推断告警文案和户外阈值校准。
- Build 15 的 BLE 事件在线上传、手机断网后重启保留、联网补报和队列计数。
- Build 16 的指令中心分组与标签、审计折叠详情、设备会话计数和服务设置只读字段。
- 远程关锁的新版 App 端交互。
- 真实 D0 有效坐标、MapKit 标记、时间与精度字段展示。
- BLE 高级设置、RFID、外部锁、OTA 等逐项能力。

## 回归清单

1. 启动 App 不发送车辆控制指令。
2. 远程和 BLE 通道切换不触发自动补发。
3. 已在目标锁状态时按钮禁用或服务返回无需操作。
4. 结果未知时不出现自动重试。
5. 密钥不出现在日志、截图文案和构建产物配置中。
6. D0 为 `V` 时不覆盖最后有效位置，轨迹间隔超过 10 分钟时不跨缺口连线。
7. 等待期 W0 只记录不提醒，布防后同一告警的前台声音与触感至少间隔 60 秒，解除需要设备身份验证。
